import React from 'react';
import MenuPre from '../../presentations/menu/MenuPre';
// import MenuPre from "@presentations/menu/MenuPre";

const MenuContainer = (props) => {
    return (
            <MenuPre/>
    );
}

export default MenuContainer;
